defaults:
  - planner: confidence
  - mapper: incremental
  - simulator: habitat
  - scene: replica/office0
  - gui: gui
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  

hydra:  
  output_subdir: null  
  run:  
    dir: .

experiment:
  output_dir: ./experiments
  exp_id: test
  run_id : 0
  max_frames: 2   # stop after this many keyframes
  record_interval: 60
  record_rgbd: false
  record_global_path: true
  pose_output_dir: /home/ghr/fs/Junyi/proj/baselines/poses

use_gui: true
debug: false